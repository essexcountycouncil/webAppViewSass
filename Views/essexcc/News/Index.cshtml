@using EssexWebapp.Models
@using EssexWebapp.ViewModels
@model NewsroomViewModel
@{
    ViewData["og:title"] = "News";
    ViewData["Title"] = "News";
    Layout = "../Shared/_Layout.cshtml";
}

@section Breadcrumbs {
    <partial name="Breadcrumb" model='new List<Crumb>()'/>
}


    <div class="main only">
        <section class="intro">
            <h1 class="page-heading">News</h1>
        </section>
        @*<div class="header-right-button grid-30 tablet-grid-30 mobile-grid-100">
            @if (Model.Newsroom.EmailAlerts)
            {

                <stock-button as-link="true" class="button-outline-white-transparent" href="@Model.EmailAlertsUrl" target="_blank"><i class="fa fa-envelope-o fa-1x" aria-hidden="true"></i>Email alerts</stock-button>
            }
            else
            {
                <stock-button as-link="true" class="button-outline-white-transparent" href="@Url.Action("Rss", "News")"><i class="fa fa-rss fa-1x" aria-hidden="true"></i>News RSS feed</stock-button>
            }
        </div>*@

        <section class="content">
            
                <partial name="NewsListing" model='Model' />
            
        </section>
    </div>
<script type="text/javascript">
    require(['startup', 'favourites', 'expandinglinks','primaryfilter', 'filters', 'viewmoreslider'],
        function (startup, favourites, expandinglinks, primaryfilter, filters, viewmoreslider) {
            favourites.Init();         
            startup.Init();
            expandinglinks.Init();
            primaryfilter.Init();
            filters.Init();            
            viewmoreslider.Init();
        })
</script>